<!DOCTYPE html>
<html>
  <head>
    <title>Moviebook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <dl>
      <dd><%= link_to 'Home', root_path %></dd>
      <dd><%= link_to 'Adicionar filme', new_movie_path %></dd>
      <dd><%= link_to 'Adicionar gênero', new_movie_genre_path %></dd>
      <dd><%= link_to 'Adicionar diretor', new_director_path %></dd>
      </dl>
    </nav>
    <%= yield %>
  </body>
</html>
